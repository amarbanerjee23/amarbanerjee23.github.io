{
  "name": "dr-amar-banerjee-portfolio-analytics",
  "private": true,
  "version": "1.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy && wrangler d1 execute DB --remote --file=./schema.sql --yes",
    "db:migrate": "wrangler d1 execute DB --remote --file=./schema.sql --yes"
  },
  "devDependencies": {
    "wrangler": "^4.102.0"
  }
}
